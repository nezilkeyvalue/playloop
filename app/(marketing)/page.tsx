// Placeholder landing page — Track C (builder UI) replaces this with the
// real URL-input landing flow described in build spec §15.
export default function LandingPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-4xl font-semibold">PlayLoop</h1>
      <p className="max-w-md text-ink/70">
        Make your brand playable in 60 seconds. Builder UI under construction.
      </p>
    </main>
  );
}
